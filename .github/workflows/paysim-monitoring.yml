name: Full PaySim monitoring audit

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/paysim-monitoring.yml'
      - 'scripts/run_paysim_monitoring.py'
      - 'src/fraud_decisioning/paysim_monitoring.py'
      - 'src/fraud_decisioning/paysim_full.py'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/paysim-monitoring.yml'
      - 'scripts/run_paysim_monitoring.py'
      - 'src/fraud_decisioning/paysim_monitoring.py'
      - 'src/fraud_decisioning/paysim_full.py'

permissions:
  contents: write

jobs:
  paysim-monitoring:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    env:
      PYTHONUNBUFFERED: '1'
      OMP_NUM_THREADS: '2'
      OPENBLAS_NUM_THREADS: '2'
      MKL_NUM_THREADS: '2'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Download canonical PaySim parquet shards
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p data/raw/paysim
          curl --fail --location --retry 5 --retry-all-errors \
            -o data/raw/paysim/0.parquet \
            'https://huggingface.co/api/datasets/kohdified/synthetic-financial-data/parquet/default/train/0.parquet'
          curl --fail --location --retry 5 --retry-all-errors \
            -o data/raw/paysim/1.parquet \
            'https://huggingface.co/api/datasets/kohdified/synthetic-financial-data/parquet/default/train/1.parquet'

      - name: Run threshold and recipient monitoring audit
        run: |
          python scripts/run_paysim_monitoring.py \
            --parquet-glob 'data/raw/paysim/*.parquet' \
            --out results/paysim_monitoring

      - name: Show aggregate monitoring results
        run: |
          cat results/paysim_monitoring/summary.json
          cat results/paysim_monitoring/threshold_budget_drift.csv
          cat results/paysim_monitoring/future_budget_windows.csv
          cat results/paysim_monitoring/future_budget_rethreshold_oracle.csv
          cat results/paysim_monitoring/recipient_signal_audit.csv

      - uses: actions/upload-artifact@v4
        with:
          name: paysim-monitoring-aggregate-results
          path: results/paysim_monitoring
          if-no-files-found: error
          retention-days: 30

      - name: Commit aggregate monitoring results
        if: github.event_name == 'push'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -- results/paysim_monitoring
          if git diff --cached --quiet; then
            echo 'No aggregate monitoring result changes to commit.'
            exit 0
          fi
          git commit -m 'data: add full PaySim monitoring audit results'
          git push
